<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">

        <a class="navbar-brand" href="/?group=all">Spiritus Santos</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                {% for groupFilter in groups %}
                    {% if groupFilter.visible %}
                        <li class="nav-item">
                            <a href="{% if groupFilter.slug %}/reis/{{ groupFilter.slug }}{% else %}/?group={{ groupFilter.id }}{% endif %}"
                               class="nav-link {% if group is defined and group == groupFilter.id %}active{% endif %}">
                                {{ groupFilter.title }}
                            </a>
                        </li>
                    {% endif %}
                {% endfor %}

                {% if activityMaps is defined %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Kaarten
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            {% for activityMap in activityMaps %}
                                <a href="/kaart/{{ activityMap.slug }}"
                                   class="dropdown-item">
                                    {{ activityMap.name }}
                                </a>
                            {% endfor %}
                        </div>
                    </li>
                {% endif %}

                {#<li class="nav-item">#}
                    {#<a href="/voies-vertes" class="nav-link {% if group == 'voies' %}active{% endif %}">#}
                        {#Voies Vertes#}
                    {#</a>#}
                {#</li>#}

            </ul>
        </div>
    </div>
</nav>
